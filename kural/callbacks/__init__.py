"""kural callbacks: optional OpenTelemetry tracing (guarded, no-op when the SDK
is absent) so each ADK span — qualify, the parallel research fan-out, each
write/fact-check loop turn, the publish gate — can be surfaced locally."""
