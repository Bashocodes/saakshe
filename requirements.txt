# saakshe — runtime dependencies (Python 3.10–3.13).
#
# The four quadrants (manas / arivu / kalai / kural) + witness are imported from the
# repo root via PYTHONPATH=. — arivu is bootstrapped onto sys.path by common/__init__.py
# — so they are NOT pip-installed; only third-party deps live here.
# Versions pinned to the verified working set (2026-06-08).

google-adk==1.34.3
google-cloud-aiplatform[adk,agent-engines]>=1.93.0
google-genai==1.75.0
anthropic[vertex]==0.107.1
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1
websockets==15.0.1
pyjwt[crypto]==2.13.0

# --- dev / test ---
pytest==9.0.3
pytest-asyncio==1.4.0

# kalai media crew (deterministic FX + HDR pipeline)
numpy
Pillow
